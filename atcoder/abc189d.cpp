#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// TODO: solve

int main(){

    int N;
    cin >> N;
    vector<string> str_dataset(N);
    for (int i = 0; i < N; i++) cin >> str_dataset.at(i);
    


    return 0;
}