#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// TODO: solve (already read a editorial)

int main(){

    ll N, M;
    cin >> N >> M;
    vector<string> ans_list(N);
    for (ll i = 0; i < N; i++){
        cin >> ans_list.at(i);
    }

    ll ans = 0;
    for (ll i = 0; i < N; i++){

    }

    cout << ans << endl;
    return 0;
}