#include <bits/stdc++.h>
using namespace std;

int main(){

    int num, target;
    cin >> num >> target;

    int i, j, temp;
    vector<int> array(num);

    cout << "--- input array ---" << endl;
    for (i=0; i<num; i++){
        array.at(i) = i+3;
        cout << array.at(i) << " ";
    }
    cout << endl;
    
    int mid, left, right;
    left = 0;
    right = num-1;
    mid = (left+right)/2;

    while (left<right){
        if ()
    }
}