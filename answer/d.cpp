// Pond

#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// TODO: read edtorial and solve

int main(){

    int N, K;
    cin >> N >> K;
    vector<vector<ll>> A(N, vector<ll>(N));
    for (int i = 0; i < N; i++){
        for (int j = 0; j < N; j++){
            cin >> A.at(i).at(j);
        }
    }


}